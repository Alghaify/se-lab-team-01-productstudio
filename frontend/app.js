const input=document.querySelector('#upload');const img=document.querySelector('#preview');const empty=document.querySelector('#empty');input.addEventListener('change',()=>{const file=input.files[0];if(!file)return;if(!['image/png','image/jpeg','image/webp'].includes(file.type)){alert('صيغة الصورة غير مدعومة');input.value='';return}img.src=URL.createObjectURL(file);img.style.display='block';empty.style.display='none';});
